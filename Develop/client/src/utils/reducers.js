import { useReducer } from 'react';
//import { actions } from './actions';

export const reducer = (state, action) => {
  switch (action.type) {
    case ACTION:
      return {
        ...state,
        
      }
  }
}